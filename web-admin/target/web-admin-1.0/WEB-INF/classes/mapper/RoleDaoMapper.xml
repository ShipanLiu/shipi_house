<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shipi.dao.RoleDao">
<!--    用于select 查询公用抽取的 列-->
    <sql id="columns">
        select id, role_name, role_code, description, create_time, update_time, is_delated
    </sql>

<!--    query all,   RoleDao 接口 里面的 findAll 方法-->
    <select id="findAll" resultType="com.shipi.entity.Role">
        <include refid="columns"></include>
        from acl_role
        where is_deleted = 0
    </select>
</mapper>