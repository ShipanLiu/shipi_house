<?xml version="1.0" encoding="UTF-8"?>

<!--这个 包是 负责  controller的-->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

<!--     定向 扫描的 package-->
    <context:component-scan base-package="com.shipi.controller"></context:component-scan>

<!--    避免 拦截 静态资源-->
    <mvc:default-servlet-handler/>
<!--    开启 mvc 注解-->
    <mvc:annotation-driven>
        <!-- config a message converter  -->
        <mvc:message-converters register-defaults="true">
            <bean class="com.alibaba.fastjson.support.spring.FastJsonHttpMessageConverter">
                <property name="supportedMediaTypes">
                    <list>
                        <value>text/html;charset=UTF-8</value>
                        <value>application/json</value>
                    </list>
                </property>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>

<!--    Spring 的 view解析器-->
    <bean class="org.thymeleaf.spring5.view.ThymeleafViewResolver" id="viewResolver">
<!--        set the encoding-->
        <property name="characterEncoding" value="UTF-8"></property>
<!--        set the engine, 下面 定义 templateEngine -->
        <property name="templateEngine" ref="templateEngine"></property>
    </bean>

<!--    define the template Engine-->
    <bean id="templateEngine" class="org.thymeleaf.spring5.SpringTemplateEngine">
<!--      ref  一个 templateResolver(后面 会定义到)  -->
        <property name="templateResolver" ref="templateResolver"></property>
    </bean>

<!--    define the template resolver-->
    <bean id="templateResolver" class="org.thymeleaf.spring5.templateresolver.SpringResourceTemplateResolver">
<!--        config prefix, 之前 叫 /WEB-INF/pages/-->
        <property name="prefix" value="/WEB-INF/templates/"></property>
<!--        config suffix-->
        <property name="suffix" value=".html"></property>
<!--        config  encoding way-->
        <property name="characterEncoding" value="UTF-8"></property>
<!--        set cache to NULL-->
        <property name="cacheable" value="false"></property>

<!--        配置模板模式： HTML5: 代表严格    LEGACYHTML5：  宽松模式-->
        <property name="templateMode" value="LEGANCYHTML5"></property>

    </bean>


</beans>